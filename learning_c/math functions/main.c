#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int main() {

  // int x = 2;
  // int x = 9;
  // float x = 3.99;
  // int x = -3;
  float x = 45;

  // x = sqrt(x);
  // x = pow(x, 4);
  // x = round(x);
  // x = ceil(x); // this round up
  // x = floor(x); // rounds down
  // x = abs(x); // gives you the distance from 0
  // x = log(x);
  // x = sin(x);
  // x = cos(x);
  x = tan(x);

  // printf("%d", x);
  // printf("%.0f\n", x);
  printf("%f", x);

  return 0;
}
