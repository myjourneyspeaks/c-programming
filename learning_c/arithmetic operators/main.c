#include <stdio.h>

int main() {

  // arithmetic operators = + - * / % ++ --

  // int x = 20;
  // int y = 15;
  // int z = 0;
  // float y = 15;
  // float z = 0;

  int x = 10;
  int y = 2;
  int z = 0;

  // z = x + y;
  // z = x - y;
  // z = x * y;
  // z = x / y;
  // z = x % y;
  // x++;
  // y--;

  // ==== augmented assignment operators =====
  // x += 2;
  // x -= 3;
  // x *= 2;
  x /= 2;

  printf("\n%d\n", x);

  return 0;
}
