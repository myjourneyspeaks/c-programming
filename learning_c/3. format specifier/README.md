# Format Specifer

Format specifer are special tokens that being with a % symbol, followed by a character that specifies the datatype and optional modifiers (width, precision, flags). They control how data is displayed or interpreted.
