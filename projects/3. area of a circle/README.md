# What to avoid:

- avoid unnecessary function calls in this case `pow()` function.
- why: for clearer simple powers and it is fast.

## Save `pow()` for:

- non-integer exponents
- complex math
- when clarity beats performance
