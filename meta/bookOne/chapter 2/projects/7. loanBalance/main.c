/* A program that calculates the remaining balance on a loan after first, second
 * and third monthly payments */

#include <stdio.h>

int main(void) {

  float amount = 0.0f, interestRate = 0.0f, monthlyPayments = 0.0f;

  return 0;
}
