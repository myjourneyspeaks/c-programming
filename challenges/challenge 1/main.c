#include <stdio.h>

int main() {

  // Declaration
  int a = 5;
  int x = 1;
  int y = 5;
  int f = 0;

  // formula
  f = (a - x) * (x - y);

  printf("a = %d, x = %d, y = %d \n", a, x, y);
  printf(" f = %d", f);

  return 0;
}
